<p><%= @resource.email %>さま</p>

<p>サインインに失敗した回数が多すぎて、アカウントがロックされました。</p>

<p>下のリンクをクリックすると、アカウントのロックが解除されます。</p>

<p><%= link_to 'Unlock my account', unlock_url(@resource, unlock_token: @token) %></p>
